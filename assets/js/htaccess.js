var mod = null;
var rotabe = null;
var alterarUrl = true;

function montarHtaccess() {
  // var nameComponent = document.getElementById("nameComponent").value;

  // if (alterarUrl) {
  //   document.getElementById("rotabe").value = nameComponent;
  //   rotabe = nameComponent;
  // }
  // mod = nameComponent;
  // configurarHtacces();
}

function configurarHtacces() {
  // document.getElementById("confihtacces").value = "RewriteRule ^(" + rotabe + ")$ index.php?mod=" + mod + " [NC,L]";

}

function alterarUrlAmigavel() {
  // alterarUrl = false;
  // rotabe = document.getElementById("rotabe").value;
  // configurarHtacces();
}